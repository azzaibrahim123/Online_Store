import 'package:flutter/material.dart';

     Widget  appbar()=>AppBar(
            backgroundColor: Colors.white,
            elevation: 0.0,
            actions: [
              IconButton(
                  onPressed: () {},
                  icon: Icon(
                    Icons.search,
                    size: 25,
                    color: Colors.black,
                  ))
            ],
          );